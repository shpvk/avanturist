# Template for .env. Copy to .env and fill in local values.
# PostgreSQL (docker-compose.yml + apps/backend)
POSTGRES_HOST=localhost
POSTGRES_PORT=5430
POSTGRES_USER=buildverdict
POSTGRES_PASSWORD=change_me
POSTGRES_DB=buildverdict

# Backend API
BACKEND_PORT=3001
FRONTEND_ORIGIN=http://localhost:3000
