{
  "name": "build-verdict",
  "private": true,
  "scripts": {
    "frontend": "pnpm --dir apps/frontend dev",
    "frontend:build": "pnpm --dir apps/frontend build",
    "backend": "pnpm --dir apps/backend/core dev",
    "backend:build": "pnpm --dir apps/backend/core build"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
